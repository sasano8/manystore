---
from: dotfiles
role: supervisor
type: instruction
priority: high
date: 2026-06-20
---

## 指示
M003 の CI を追加する。GitHub Actions で `make check`（ruff format-check + ruff check + pytest）を
push / PR 時に回すワークフロー（`.github/workflows/ci.yml`）を作る。

## 背景 / 受け入れ条件
- 足回り（Makefile: format / format-check / lint / test / check）は整備済み。CI 追加は安く・無害・効果が
  高いので、実効優先度を **high** と判断（supervisor）。
- 受け入れ: push で CI が起動し、`make check` 相当（`uvx ruff format --check` + `ruff check` + `uv run pytest`）
  が緑になること。uv のセットアップ（`astral-sh/setup-uv` 等）を含める。
- 完了したら progress.md の M003 を更新し、本指示ファイルは interrupt/archive/ へ退避すること。
