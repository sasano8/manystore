# 要求１

これはスーパーバイザーへの要求。
make format** make check** make test** make lint** をグローバルに許可したい。

# 要求２

conformance.py を conformancer ディレクトリ配下に整理するようする。

# 要求３

manystore.__init__.py に ファクトリメソッドが欲しい。
kv.py, file.py から直接実装バックエンドを初期化する使い方は想定していない。

SafeKeyValueStore, SafeFileStoreなどでラップすることを前提としてあるはずだ。
ラップまで面倒を見るファクトリメソッドはどこになるか。


# 要求４

list_all は iter_all を参照すべき。
iter_all は limitを受け取れるべき。

def iter_all(self) -> AsyncIterator[FileInfo]:
        return self._store.iter_all()

async def list_all(self, limit: int = 10) -> list[FileInfo]:
    return await self._store.list_all(limit)

# 要求５

conformance.py（conformancer）でテストを実行した後、
挙動を出力して、docs に保存したい。それを Makefile でキックできるように。

まずは雑に出力できればいいが、理想は以下のような表を file_storage_spec.md と kv_spec.md に出力。
Implemented などは簡略化した表現でもよい。実装状況が分かるのならそのコメントを入れてもよい。

LocalFileStorage
| | |
| list_all | Implemented |
| exists | Implemented |
| open_reader | Implemented |
| open_writer | Implemented |
| delete | Not implemented |

S3FileStorage
| | |
| list_all | Implemented |
| exists | Implemented |

# 要求６

以下は実装の１つのようなものなので別ディレクトリなどに分類していきたい。

array_storage.py
async_storage.py
async_to_sync_storage.py
safe_path.py
sync_storage.py

インターフェース・プロトコル的なものは抽出し、manystore 配下に置きたい。

# 要求７

実装としてフォールバック禁止。
問題が隠ぺいされてしまう。
